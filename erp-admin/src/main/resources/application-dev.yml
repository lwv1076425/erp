# 开发环境配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/erp?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 960930
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 10000

# 日志级别
logging:
  level:
    com.erp: debug
    com.baomidou.mybatisplus: debug
